{
  "name": "whatsapp-mcp",
  "version": "1.0.0",
  "description": "WhatsApp MCP protocol server — exposes tools to Claude/Cursor",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev":   "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node-dev": "^2.0.0"
  },
  "engines": { "node": ">=18.0.0" }
}
